#!/bin/bash
HOME_DIRS=$(fdfind -H -d 1 . $(echo $REPOS))
PWD_DIRS=$(fdfind -H . .)
DIRECTORY=$(echo $HOME_DIRS $PWD_DIRS | fzf)

if [ -d "$DIRECTORY" ]; then
    cd $DIRECTORY
elif [ -f "$DIRECTORY" ]; then
    vim $DIRECTORY
fi
